---
export interface Props {
    src: string;
    alt: string;
    class?: string;
}

const { src, alt, class: className = "" } = Astro.props;
---

<img
    src={src}
    alt={alt}
    class={`block my-8 -mx-10 w-[calc(100%+5rem)] ${className}`}
/>
