---
export interface Props {
    class?: string;
}

const { class: className = "" } = Astro.props;
---

<hr
    class={`border-none h-1 bg-gradient-to-r from-transparent via-neon-cyan via-neon-pink via-neon-yellow to-transparent my-16 rounded-sm shadow-[0_0_20px_rgba(0,255,247,0.3)] ${className}`}
/>
